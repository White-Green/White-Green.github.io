---
interface Props {
    video_id: string;
}

const { video_id } = Astro.props;
---
<iframe width="312" height="176" src={`https://ext.nicovideo.jp/thumb/${video_id}`} scrolling="no" style="border:solid 1px #ccc;" frameborder="0"><a href={`https://www.nicovideo.jp/watch/${video_id}`}>LINK</a></iframe>
